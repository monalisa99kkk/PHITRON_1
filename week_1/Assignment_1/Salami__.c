#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int X, Y, Z, total_salami = 0;
    scanf("%d%d%d",&X,&Y,&Z);
    total_salami = X + Y + Z;
    printf("%d",total_salami);

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}